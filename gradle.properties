kotlin.code.style=official
android.useAndroidX=true

# Tuned for an 8GB machine: keep peak concurrent memory low rather than
# large. The daemon heap plus the Kotlin daemon plus an IDE must all fit
# in physical RAM, or native allocations start failing (hs_err_pid crashes).
org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=1g
kotlin.daemon.jvmargs=-Xmx1g
org.gradle.workers.max=2

sdkGroup=com.thelightphone
sdkVersion=0.1.0
android.defaults.buildfeatures.resvalues=true
android.sdk.defaultTargetSdkToCompileSdkIfUnset=false
android.enableAppCompileTimeRClass=false
android.usesSdkInManifest.disallowed=false
android.uniquePackageNames=false
android.dependency.useConstraints=true
android.r8.strictFullModeForKeepRules=false
android.r8.optimizedResourceShrinking=false
android.builtInKotlin=false
android.newDsl=false
